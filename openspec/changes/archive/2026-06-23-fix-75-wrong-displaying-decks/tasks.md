## 1. GUI Layout-Anpassungen

- [x] 1.1 Listener für `recentContainer` in `HomepageView.java` implementieren
- [x] 1.2 Listener für `deckGrid` in `HomeView.java` implementieren
- [x] 1.3 Listener für `cardsGrid` in `FlashcardsView.java` implementieren

## 2. Testen & Verifizieren

- [x] 2.1 Kompilieren des Frontends mit `mvn clean compile` prüfen
- [x] 2.2 Bestehende Tests mit `mvn test` ausführen und validieren
- [x] 2.3 Überprüfung des fehlerfreien Spaltenumbruchs bei variierenden Fensterbreiten
