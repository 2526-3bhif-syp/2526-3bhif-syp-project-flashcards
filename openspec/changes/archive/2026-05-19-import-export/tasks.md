## 1. Export

- [x] 1.1 Export-Menüpunkt in `NavbarView.java` hinzufügen
- [x] 1.2 `FileChooser` für Speicherort öffnen
- [x] 1.3 Ausgewählte Stapel als JSON serialisieren und schreiben (inkl. Base64-Medien)

## 2. Import

- [x] 2.1 Import-Menüpunkt in `NavbarView.java` hinzufügen (getrennt von Export und Karten-Import)
- [x] 2.2 `FileChooser` für JSON-Datei öffnen
- [x] 2.3 JSON parsen und validieren, bevor der Nutzer nach Optionen gefragt wird
- [x] 2.4 Fehlermeldung bei ungültigem JSON (kein Stack Trace in Log)
- [x] 2.5 Importierte Stapel dem Modell hinzufügen und UI aktualisieren
